// The closing ink band is shared site-wide; this path is kept so the 28 routes
// that already import FooterV2 pick up the redesign without edits.
export { default } from '@/components/feature/SiteFooter';
